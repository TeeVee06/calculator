function add() {
    c.value=parseInt(a.value)+parseInt(b.value);
}
function subtract() {
    c.value=parseInt(a.value)-parseInt(b.value);
}
function multiply() {
    c.value=parseInt(a.value)*parseInt(b.value);
}
function divide () {
    c.value=parseInt(a.value)/parseInt(b.value);
}
